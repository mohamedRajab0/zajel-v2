import React from "react";
import { useHistory } from "react-router-dom";

function ContentChat({ friend }) {
  const history = useHistory();

  const handleChatClick = () => {
    history.push(`/chat/${friend.roomName}`);
  };
}
